# Data
# events.csv is committed so the deployed app can serve real sample incidents
backend/data/processed/features_train.csv
backend/data/processed/features_test.csv

# Model artifacts are committed so the app runs without retraining (needed on Railway)
# backend/model_artifacts/*.pkl

# Python cache & envs
__pycache__/
*.pyc
.venv/
.venv*/
venv/
env/

# Node/Vite dependencies
frontend/node_modules/
frontend/dist/
frontend/.eslintcache

# Local dev logs & artifacts
*.log
frontend-render.png

# OS files
.DS_Store
Thumbs.db
